// import "package:flutter/material.dart";
import 'first.dart' as fst;
import 'second.dart' show disp1;

void main() {
  // runApp(MaterialApp(home: Scaffold(appBar: AppBar(), body: Center())));
  disp1();
  fst.disp2();
}
